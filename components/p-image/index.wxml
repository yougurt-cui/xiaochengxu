<view class="frame {{mode === 'widthFix' ? 'fluid' : ''}} {{!loaded ? 'pending' : ''}} image-class"><image wx:if="{{displaySrc}}" class="media {{loaded ? 'ready' : ''}}" src="{{displaySrc}}" data-src="{{src}}" mode="{{mode}}" lazy-load="{{lazyLoad}}" bindload="onLoad" binderror="onError" aria-label="{{alt}}" /><view wx:if="{{!loaded}}" class="placeholder" aria-hidden="true"><view wx:if="{{src && !failed}}" class="shimmer"></view><p-icon wx:else name="image" tone="muted" size="36" /></view></view>
